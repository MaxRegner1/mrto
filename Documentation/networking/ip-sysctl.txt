/proc/sys/net/ipv4/* Variables:

ip_forward - BOOLEAN
	0 - disabled (default)
	not 0 - enabled

	Forward Packets between interfaces.

	This variable is special, its change resets all configuration
	parameters to their default state (RFC1122 for hosts, RFC1812
	for routers)

ip_default_ttl - INTEGER
	Default value of TTL field (Time To Live) for outgoing (but not
	forwarded) IP packets. Should be between 1 and 255 inclusive.
	Default: 64 (as recommended by RFC1700)

ip_no_pmtu_disc - INTEGER
	Disable Path MTU Discovery. If enabled in mode 1 and a
	fragmentation-required ICMP is received, the PMTU to this
	destination will be set to min_pmtu (see below). You will need
	to raise min_pmtu to the smallest interface MTU on your system
	manually if you want to avoid locally generated fragments.

	In mode 2 incoming Path MTU Discovery messages will be
	discarded. Outgoing frames are handled the same as in mode 1,
	implicitly setting IP_PMTUDISC_DONT on every created socket.

	Mode 3 is a hardend pmtu discover mode. The kernel will only
	accept fragmentation-needed errors if the underlying protocol
	can verify them besides a plain socket lookup. Current
	protocols for which pmtu events will be honored are TCP, SCTP
	and DCCP as they verify e.g. the sequence number or the
	association. This mode should not be enabled globally but is
	only intended to secure e.g. name servers in namespaces where
	TCP path mtu must still work but path MTU information of other
	protocols should be discarded. If enabled globally this mode
	could break other protocols.

	Possible values: 0-3
	Default: FALSE

min_pmtu - INTEGER
	default 552 - minimum discovered Path MTU

ip_forward_use_pmtu - BOOLEAN
	By default we don't trust protocol path MTUs while forwarding
	because they could be easily forged and can lead to unwanted
	fragmentation by the router.
	You only need to enable this if you have user-space software
	which tries to discover path mtus by itself and depends on the
	kernel honoring this information. This is normally not the
	case.
	Default: 0 (disabled)
	Possible values:
	0 - disabled
	1 - enabled

fwmark_reflect - BOOLEAN
	Controls the fwmark of kernel-generated IPv4 reply packets that are not
	associated with a socket for example, TCP RSTs or ICMP echo replies).
	If unset, these packets have a fwmark of zero. If set, they have the
	fwmark of the packet they are replying to.
	Default: 0

fib_multipath_use_neigh - BOOLEAN
	Use status of existing neighbor entry when determining nexthop for
	multipath routes. If disabled, neighbor information is not used and
	packets could be directed to a failed nexthop. Only valid for kernels
	built with CONFIG_IP_ROUTE_MULTIPATH enabled.
	Default: 0 (disabled)
	Possible values:
	0 - disabled
	1 - enabled

fib_multipath_hash_policy - INTEGER
	Controls which hash policy to use for multipath routes. Only valid
	for kernels built with CONFIG_IP_ROUTE_MULTIPATH enabled.
	Default: 0 (Layer 3)
	Possible values:
	0 - Layer 3
	1 - Layer 4

route/max_size - INTEGER
	Maximum number of routes allowed in the kernel.  Increase
	this when using large numbers of interfaces and/or routes.
	From linux kernel 3.6 onwards, this is deprecated for ipv4
	as route cache is no longer used.

neigh/default/gc_thresh1 - INTEGER
	Minimum number of entries to keep.  Garbage collector will not
	purge entries if there are fewer than this number.
	Default: 128

neigh/default/gc_thresh2 - INTEGER
	Threshold when garbage collector becomes more aggressive about
	purging entries. Entries older than 5 seconds will be cleared
	when over this number.
	Default: 512

neigh/default/gc_thresh3 - INTEGER
	Maximum number of neighbor entries allowed.  Increase this
	when using large numbers of interfaces and when communicating
	with large numbers of directly-connected peers.
	Default: 1024

neigh/default/unres_qlen_bytes - INTEGER
	The maximum number of bytes which may be used by packets
	queued for each	unresolved address by other network layers.
	(added in linux 3.3)
	Setting negative value is meaningless and will return error.
	Default: SK_WMEM_MAX, (same as net.core.wmem_default).
		Exact value depends on architecture and kernel options,
		but should be enough to allow queuing 256 packets
		of medium size.

neigh/default/unres_qlen - INTEGER
	The maximum number of packets which may be queued for each
	unresolved address by other network layers.
	(deprecated in linux 3.3) : use unres_qlen_bytes instead.
	Prior to linux 3.3, the default value is 3 which may cause
	unexpected packet loss. The current default value is calculated
	according to default value of unres_qlen_bytes and true size of
	packet.
	Default: 101

mtu_expires - INTEGER
	Time, in seconds, that cached PMTU information is kept.

min_adv_mss - INTEGER
	The advertised MSS depends on the first hop route MTU, but will
	never be lower than this setting.

IP Fragmentation:

ipfrag_high_thresh - LONG INTEGER
	Maximum memory used to reassemble IP fragments.

ipfrag_low_thresh - LONG INTEGER
	(Obsolete since linux-4.17)
	Maximum memory used to reassemble IP fragments before the kernel
	begins to remove incomplete fragment queues to free up resources.
	The kernel still accepts new fragments for defragmentation.

ipfrag_time - INTEGER
	Time in seconds to keep an IP fragment in memory.

ipfrag_max_dist - INTEGER
	ipfrag_max_dist is a non-negative integer value which defines the
	maximum "disorder" which is allowed among fragments which share a
	common IP source address. Note that reordering of packets is
	not unusual, but if a large number of fragments arrive from a source
	IP address while a particular fragment queue remains incomplete, it
	probably indicates that one or more fragments belonging to that queue
	have been lost. When ipfrag_max_dist is positive, an additional check
	is done on fragments before they are added to a reassembly queue - if
	ipfrag_max_dist (or more) fragments have arrived from a particular IP
	address between additions to any IP fragment queue using that source
	address, it's presumed that one or more fragments in the queue are
	lost. The existing fragment queue will be dropped, and a new one
	started. An ipfrag_max_dist value of zero disables this check.

	Using a very small value, e.g. 1 or 2, for ipfrag_max_dist can
	result in unnecessarily dropping fragment queues when normal
	reordering of packets occurs, which could lead to poor application
	performance. Using a very large value, e.g. 50000, increases the
	likelihood of incorrectly reassembling IP fragments that originate
	from different IP datagrams, which could result in data corruption.
	Default: 64

INET peer storage:

inet_peer_threshold - INTEGER
	The approximate size of the storage.  Starting from this threshold
	entries will be thrown aggressively.  This threshold also determines
	entries' time-to-live and time intervals between garbage collection
	passes.  More entries, less time-to-live, less GC interval.

inet_peer_minttl - INTEGER
	Minimum time-to-live of entries.  Should be enough to cover fragment
	time-to-live on the reassembling side.  This minimum time-to-live  is
	guaranteed if the pool size is less than inet_peer_threshold.
	Measured in seconds.

inet_peer_maxttl - INTEGER
	Maximum time-to-live of entries.  Unused entries will expire after
	this period of time if there is no memory pressure on the pool (i.e.
	when the number of entries in the pool is very small).
	Measured in seconds.

TCP variables:

somaxconn - INTEGER
	Limit of socket listen() backlog, known in userspace as SOMAXCONN.
	Defaults to 128.  See also tcp_max_syn_backlog for additional tuning
	for TCP sockets.

tcp_abort_on_overflow - BOOLEAN
	If listening service is too slow to accept new connections,
	reset them. Default state is FALSE. It means that if overflow
	occurred due to a burst, connection will recover. Enable this
	option _only_ if you are really sure that listening daemon
	cannot be tuned to accept connections faster. Enabling this
	option can harm clients of your server.

tcp_adv_win_scale - INTEGER
	Count buffering overhead as bytes/2^tcp_adv_win_scale
	(if tcp_adv_win_scale > 0) or bytes-bytes/2^(-tcp_adv_win_scale),
	if it is <= 0.
	Possible values are [-31, 31], inclusive.
	Default: 1

tcp_allowed_congestion_control - STRING
	Show/set the congestion control choices available to non-privileged
	processes. The list is a subset of those listed in
	tcp_available_congestion_control.
	Default is "reno" and the default setting (tcp_congestion_control).

tcp_app_win - INTEGER
	Reserve max(window/2^tcp_app_win, mss) of window for application
	buffer. Value 0 is special, it means that nothing is reserved.
	Default: 31

tcp_autocorking - BOOLEAN
	Enable TCP auto corking :
	When applications do consecutive small write()/sendmsg() system calls,
	we try to coalesce these small writes as much as possible, to lower
	total amount of sent packets. This is done if at least one prior
	packet for the flow is waiting in Qdisc queues or device transmit
	queue. Applications can still use TCP_CORK for optimal behavior
	when they know how/when to uncork their sockets.
	Default : 1

tcp_available_congestion_control - STRING
	Shows the available congestion control choices that are registered.
	More congestion control algorithms may be available as modules,
	but not loaded.

tcp_base_mss - INTEGER
	The initial value of search_low to be used by the packetization layer
	Path MTU discovery (MTU probing).  If MTU probing is enabled,
	this is the initial MSS used by the connection.

tcp_min_snd_mss - INTEGER
	TCP SYN and SYNACK messages usually advertise an ADVMSS option,
	as described in RFC 1122 and RFC 6691.
	If this ADVMSS option is smaller than tcp_min_snd_mss,
	it is silently capped to tcp_min_snd_mss.

	Default : 48 (at least 8 bytes of payload per segment)

tcp_congestion_control - STRING
	Set the congestion control algorithm to be used for new
	connections. The algorithm "reno" is always available, but
	additional choices may be available based on kernel configuration.
	Default is set as part of kernel configuration.
	For passive connections, the listener congestion control choice
	is inherited.
	[see setsockopt(listenfd, SOL_TCP, TCP_CONGESTION, "name" ...) ]

tcp_dsack - BOOLEAN
	Allows TCP to send "duplicate" SACKs.

tcp_early_retrans - INTEGER
	Tail loss probe (TLP) converts RTOs occurring due to tail
	losses into fast recovery (draft-ietf-tcpm-rack). Note that
	TLP requires RACK to function properly (see tcp_recovery below)
	Possible values:
		0 disables TLP
		3 or 4 enables TLP
	Default: 3

tcp_ecn - INTEGER
	Control use of Explicit Congestion Notification (ECN) by TCP.
	ECN is used only when both ends of the TCP connection indicate
	support for it.  This feature is useful in avoiding losses due
	to congestion by allowing supporting routers to signal
	congestion before having to drop packets.
	Possible values are:
		0 Disable ECN.  Neither initiate nor accept ECN.
		1 Enable ECN when requested by incoming connections and
		  also request ECN on outgoing connection attempts.
		2 Enable ECN when requested by incoming connections
		  but do not request ECN on outgoing connections.
	Default: 2

tcp_ecn_fallback - BOOLEAN
	If the kernel detects that ECN connection misbehaves, enable fall
	back to non-ECN. Currently, this knob implements the fallback
	from RFC3168, section 6.1.1.1., but we reserve that in future,
	additional detection mechanisms could be implemented under this
	knob. The value	is not used, if tcp_ecn or per route (or congestion
	control) ECN settings are disabled.
	Default: 1 (fallback enabled)

tcp_fack - BOOLEAN
	Enable FACK congestion avoidance and fast retransmission.
	The value is not used, if tcp_sack is not enabled.

tcp_fin_timeout - INTEGER
	The length of time an orphaned (no longer referenced by any
	application) connection will remain in the FIN_WAIT_2 state
	before it is aborted at the local end.  While a perfectly
	valid "receive only" state for an un-orphaned connection, an
	orphaned connection in FIN_WAIT_2 state could otherwise wait
	forever for the remote to close its end of the connection.
	Cf. tcp_max_orphans
	Default: 60 seconds

tcp_frto - INTEGER
	Enables Forward RTO-Recovery (F-RTO) defined in RFC5682.
	F-RTO is an enhanced recovery algorithm for TCP retransmission
	timeouts.  It is particularly beneficial in networks where the
	RTT fluctuates (e.g., wireless). F-RTO is sender-side only
	modification. It does not require any support from the peer.

	By default it's enabled with a non-zero value. 0 disables F-RTO.

tcp_invalid_ratelimit - INTEGER
	Limit the maximal rate for sending duplicate acknowledgments
	in response to incoming TCP packets that are for an existing
	connection but that are invalid due to any of these reasons:

	  (a) out-of-window sequence number,
	  (b) out-of-window acknowledgment number, or
	  (c) PAWS (Protection Against Wrapped Sequence numbers) check failure

	This can help mitigate simple "ack loop" DoS attacks, wherein
	a buggy or malicious middlebox or man-in-the-middle can
	rewrite TCP header fields in manner that causes each endpoint
	to think that the other is sending invalid TCP segments, thus
	causing each side to send an unterminating stream of duplicate
	acknowledgments for invalid segments.

	Using 0 disables rate-limiting of dupacks in response to
	invalid segments; otherwise this value specifies the minimal
	space between sending such dupacks, in milliseconds.

	Default: 500 (milliseconds).

tcp_keepalive_time - INTEGER
	How often TCP sends out keepalive messages when keepalive is enabled.
	Default: 2hours.

tcp_keepalive_probes - INTEGER
	How many keepalive probes TCP sends out, until it decides that the
	connection is broken. Default value: 9.

tcp_keepalive_intvl - INTEGER
	How frequently the probes are send out. Multiplied by
	tcp_keepalive_probes it is time to kill not responding connection,
	after probes started. Default value: 75sec i.e. connection
	will be aborted after ~11 minutes of retries.

tcp_l3mdev_accept - BOOLEAN
	Enables child sockets to inherit the L3 master device index.
	Enabling this option allows a "global" listen socket to work
	across L3 master domains (e.g., VRFs) with connected sockets
	derived from the listen socket to be bound to the L3 domain in
	which the packets originated. Only valid when the kernel was
	compiled with CONFIG_NET_L3_MASTER_DEV.

tcp_low_latency - BOOLEAN
	This is a legacy option, it has no effect anymore.

tcp_max_orphans - INTEGER
	Maximal number of TCP sockets not attached to any user file handle,
	held by system.	If this number is exceeded orphaned connections are
	reset immediately and warning is printed. This limit exists
	only to prevent simple DoS attacks, you _must_ not rely on this
	or lower the limit artificially, but rather increase it
	(probably, after increasing installed memory),
	if network conditions require more than default value,
	and tune network services to linger and kill such states
	more aggressively. Let me to remind again: each orphan eats
	up to ~64K of unswappable memory.

tcp_max_syn_backlog - INTEGER
	Maximal number of remembered connection requests, which have not
	received an acknowledgment from connecting client.
	The minimal value is 128 for low memory machines, and it will
	increase in proportion to the memory of machine.
	If server suffers from overload, try increasing this number.

tcp_max_tw_buckets - INTEGER
	Maximal number of timewait sockets held by system simultaneously.
	If this number is exceeded time-wait socket is immediately destroyed
	and warning is printed. This limit exists only to prevent
	simple DoS attacks, you _must_ not lower the limit artificially,
	but rather increase it (probably, after increasing installed memory),
	if network conditions require more than default value.

tcp_mem - vector of 3 INTEGERs: min, pressure, max
	min: below this number of pages TCP is not bothered about its
	memory appetite.

	pressure: when amount of memory allocated by TCP exceeds this number
	of pages, TCP moderates its memory consumption and enters memory
	pressure mode, which is exited when memory consumption falls
	under "min".

	max: number of pages allowed for queueing by all TCP sockets.

	Defaults are calculated at boot time from amount of available
	memory.

tcp_min_rtt_wlen - INTEGER
	The window length of the windowed min filter to track the minimum RTT.
	A shorter window lets a flow more quickly pick up new (higher)
	minimum RTT when it is moved to a longer path (e.g., due to traffic
	engineering). A longer window makes the filter more resistant to RTT
	inflations such as transient congestion. The unit is seconds.
	Possible values: 0 - 86400 (1 day)
	Default: 300

tcp_moderate_rcvbuf - BOOLEAN
	If set, TCP performs receive buffer auto-tuning, attempting to
	automatically size the buffer (no greater than tcp_rmem[2]) to
	match the size required by the path for full throughput.  Enabled by
	default.

tcp_mtu_probing - INTEGER
	Controls TCP Packetization-Layer Path MTU Discovery.  Takes three
	values:
	  0 - Disabled
	  1 - Disabled by default, enabled when an ICMP black hole detected
	  2 - Always enabled, use initial MSS of tcp_base_mss.

tcp_probe_interval - INTEGER
	Controls how often to start TCP Packetization-Layer Path MTU
	Discovery reprobe. The default is reprobing every 10 minutes as
	per RFC4821.

tcp_probe_threshold - INTEGER
	Controls when TCP Packetization-Layer Path MTU Discovery probing
	will stop in respect to the width of search range in bytes. Default
	is 8 bytes.

tcp_no_metrics_save - BOOLEAN
	By default, TCP saves various connection metrics in the route cache
	when the connection closes, so that connections established in the
	near future can use these to set initial conditions.  Usually, this
	increases overall performance, but may sometimes cause performance
	degradation.  If set, TCP will not cache metrics on closing
	connections.

tcp_orphan_retries - INTEGER
	This value influences the timeout of a locally closed TCP connection,
	when RTO retransmissions remain unacknowledged.
	See tcp_retries2 for more details.

	The default value is 8.
	If your machine is a loaded WEB server,
	you should think about lowering this value, such sockets
	may consume significant resources. Cf. tcp_max_orphans.

tcp_recovery - INTEGER
	This value is a bitmap to enable various experimental loss recovery
	features.

	RACK: 0x1 enables the RACK loss detection for fast detection of lost
	      retransmissions and tail drops.

	Default: 0x1

tcp_reordering - INTEGER
	Initial reordering level of packets in a TCP stream.
	TCP stack can then dynamically adjust flow reordering level
	between this initial value and tcp_max_reordering
	Default: 3

tcp_max_reordering - INTEGER
	Maximal reordering level of packets in a TCP stream.
	300 is a fairly conservative value, but you might increase it
	if paths are using per packet load balancing (like bonding rr mode)
	Default: 300

tcp_retrans_collapse - BOOLEAN
	Bug-to-bug compatibility with some broken printers.
	On retransmit try to send bigger packets to work around bugs in
	certain TCP stacks.

tcp_retries1 - INTEGER
	This value influences the time, after which TCP decides, that
	something is wrong due to unacknowledged RTO retransmissions,
	and reports this suspicion to the network layer.
	See tcp_retries2 for more details.

	RFC 1122 recommends at least 3 retransmissions, which is the
	default.

tcp_retries2 - INTEGER
	This value influences the timeout of an alive TCP connection,
	when RTO retransmissions remain unacknowledged.
	Given a value of N, a hypothetical TCP connection following
	exponential backoff with an initial RTO of TCP_RTO_MIN would
	retransmit N times before killing the connection at the (N+1)th RTO.

	The default value of 15 yields a hypothetical timeout of 924.6
	seconds and is a lower bound for the effective timeout.
	TCP will effectively time out at the first RTO which exceeds the
	hypothetical timeout.

	RFC 1122 recommends at least 100 seconds for the timeout,
	which corresponds to a value of at least 8.

tcp_rfc1337 - BOOLEAN
	If set, the TCP stack behaves conforming to RFC1337. If unset,
	we are not conforming to RFC, but prevent TCP TIME_WAIT
	assassination.
	Default: 0

tcp_rmem - vector of 3 INTEGERs: min, default, max
	min: Minimal size of receive buffer used by TCP sockets.
	It is guaranteed to each TCP socket, even under moderate memory
	pressure.
	Default: 4K

	default: initial size of receive buffer used by TCP sockets.
	This value overrides net.core.rmem_default used by other protocols.
	Default: 87380 bytes. This value results in window of 65535 with
	default setting of tcp_adv_win_scale and tcp_app_win:0 and a bit
	less for default tcp_app_win. See below about these variables.

	max: maximal size of receive buffer allowed for automatically
	selected receiver buffers for TCP socket. This value does not override
	net.core.rmem_max.  Calling setsockopt() with SO_RCVBUF disables
	automatic tuning of that socket's receive buffer size, in which
	case this value is ignored.
	Default: between 87380B and 6MB, depending on RAM size.

tcp_sack - BOOLEAN
	Enable select acknowledgments (SACKS).

tcp_slow_start_after_idle - BOOLEAN
	If set, provide RFC2861 behavior and time out the congestion
	window after an idle period.  An idle period is defined at
	the current RTO.  If unset, the congestion window will not
	be timed out after an idle period.
	Default: 1

tcp_stdurg - BOOLEAN
	Use the Host requirements interpretation of the TCP urgent pointer field.
	Most hosts use the older BSD interpretation, so if you turn this on
	Linux might not communicate correctly with them.
	Default: FALSE

tcp_synack_retries - INTEGER
	Number of times SYNACKs for a passive TCP connection attempt will
	be retransmitted. Should not be higher than 255. Default value
	is 5, which corresponds to 31seconds till the last retransmission
	with the current initial RTO of 1second. With this the final timeout
	for a passive TCP connection will happen after 63seconds.

tcp_syncookies - BOOLEAN
	Only valid when the kernel was compiled with CONFIG_SYN_COOKIES
	Send out syncookies when the syn backlog queue of a socket
	overflows. This is to prevent against the common 'SYN flood attack'
	Default: 1

	Note, that syncookies is fallback facility.
	It MUST NOT be used to help highly loaded servers to stand
	against legal connection rate. If you see SYN flood warnings
	in your logs, but investigation	shows that they occur
	because of overload with legal connections, you should tune
	another parameters until this warning disappear.
	See: tcp_max_syn_backlog, tcp_synack_retries, tcp_abort_on_overflow.

	syncookies seriously violate TCP protocol, do not allow
	to use TCP extensions, can result in serious degradation
	of some services (f.e. SMTP relaying), visible not by you,
	but your clients and relays, contacting you. While you see
	SYN flood warnings in logs not being really flooded, your server
	is seriously misconfigured.

	If you want to test which effects syncookies have to your
	network connections you can set this knob to 2 to enable
	unconditionally generation of syncookies.

tcp_fastopen - INTEGER
	Enable TCP Fast Open (RFC7413) to send and accept data in the opening
	SYN packet.

	The client support is enabled by flag 0x1 (on by default). The client
	then must use sendmsg() or sendto() with the MSG_FASTOPEN flag,
	rather than connect() to send data in SYN.

	The server support is enabled by flag 0x2 (off by default). Then
	either enable for all listeners with another flag (0x400) or
	enable individual listeners via TCP_FASTOPEN socket option with
	the option value being the length of the syn-data backlog.

	The values (bitmap) are
	  0x1: (client) enables sending data in the opening SYN on the client.
	  0x2: (server) enables the server support, i.e., allowing data in
			a SYN packet to be accepted and passed to the
			application before 3-way handshake finishes.
	  0x4: (client) send data in the opening SYN regardless of cookie
			availability and without a cookie option.
	0x200: (server) accept data-in-SYN w/o any cookie option present.
	0x400: (server) enable all listeners to support Fast Open by
			default without explicit TCP_FASTOPEN socket option.

	Default: 0x1

	Note that that additional client or server features are only
	effective if the basic support (0x1 and 0x2) are enabled respectively.

tcp_fastopen_blackhole_timeout_sec - INTEGER
	Initial time period in second to disable Fastopen on active TCP sockets
	when a TFO firewall blackhole issue happens.
	This time period will grow exponentially when more blackhole issues
	get detected right after Fastopen is re-enabled and will reset to
	initial value when the blackhole issue goes away.
	By default, it is set to 1hr.

tcp_fwmark_accept - BOOLEAN
	If set, incoming connections to listening sockets that do not have a
	socket mark will set the mark of the accepting socket to the fwmark of
	the incoming SYN packet. This will cause all packets on that connection
	(starting from the first SYNACK) to be sent with that fwmark. The
	listening socket's mark is unchanged. Listening sockets that already
	have a fwmark set via setsockopt(SOL_SOCKET, SO_MARK, ...) are
	unaffected.
	Default: 0

tcp_syn_retries - INTEGER
	Number of times initial SYNs for an active TCP connection attempt
	will be retransmitted. Should not be higher than 127. Default value
	is 6, which corresponds to 63seconds till the last retransmission
	with the current initial RTO of 1second. With this the final timeout
	for an active TCP connection attempt will happen after 127seconds.

tcp_timestamps - INTEGER
Enable timestamps as defined in RFC1323.
	0: Disabled.
	1: Enable timestamps as defined in RFC1323 and use random offset for
	each connection rather than only using the current time.
	2: Like 1, but without random offsets.
	Default: 1

tcp_min_tso_segs - INTEGER
	Minimal number of segments per TSO frame.
	Since linux-3.12, TCP does an automatic sizing of TSO frames,
	depending on flow rate, instead of filling 64Kbytes packets.
	For specific usages, it's possible to force TCP to build big
	TSO frames. Note that TCP stack might split too big TSO packets
	if available window is too small.
	Default: 2

tcp_pacing_ss_ratio - INTEGER
	sk->sk_pacing_rate is set by TCP stack using a ratio applied
	to current rate. (current_rate = cwnd * mss / srtt)
	If TCP is in slow start, tcp_pacing_ss_ratio is applied
	to let TCP probe for bigger speeds, assuming cwnd can be
	doubled every other RTT.
	Default: 200

tcp_pacing_ca_ratio - INTEGER
	sk->sk_pacing_rate is set by TCP stack using a ratio applied
	to current rate. (current_rate = cwnd * mss / srtt)
	If TCP is in congestion avoidance phase, tcp_pacing_ca_ratio
	is applied to conservatively probe for bigger throughput.
	Default: 120

tcp_tso_win_divisor - INTEGER
	This allows control over what percentage of the congestion window
	can be consumed by a single TSO frame.
	The setting of this parameter is a choice between burstiness and
	building larger TSO frames.
	Default: 3

tcp_tw_reuse - BOOLEAN
	Allow to reuse TIME-WAIT sockets for new connections when it is
	safe from protocol viewpoint. Default value is 0.
	It should not be changed without advice/request of technical
	experts.

tcp_window_scaling - BOOLEAN
	Enable window scaling as defined in RFC1323.

tcp_wmem - vector of 3 INTEGERs: min, default, max
	min: Amount of memory reserved for send buffers for TCP sockets.
	Each TCP socket has rights to use it due to fact of its birth.
	Default: 4K

	default: initial size of send buffer used by TCP sockets.  This
	value overrides net.core.wmem_default used by other protocols.
	It is usually lower than net.core.wmem_default.
	Default: 16K

	max: Maximal amount of memory allowed for automatically tuned
	send buffers for TCP sockets. This value does not override
	net.core.wmem_max.  Calling setsockopt() with SO_SNDBUF disables
	automatic tuning of that socket's send buffer size, in which case
	this value is ignored.
	Default: between 64K and 4MB, depending on RAM size.

tcp_notsent_lowat - UNSIGNED INTEGER
	A TCP socket can control the amount of unsent bytes in its write queue,
	thanks to TCP_NOTSENT_LOWAT socket option. poll()/select()/epoll()
	reports POLLOUT events if the amount of unsent bytes is below a per
	socket value, and if the write queue is not full. sendmsg() will
	also not add new buffers if the limit is hit.

	This global variable controls the amount of unsent data for
	sockets not using TCP_NOTSENT_LOWAT. For these sockets, a change
	to the global variable has immediate effect.

	Default: UINT_MAX (0xFFFFFFFF)

tcp_workaround_signed_windows - BOOLEAN
	If set, assume no receipt of a window scaling option means the
	remote TCP is broken and treats the window as a signed quantity.
	If unset, assume the remote TCP is not broken even if we do
	not receive a window scaling option from them.
	Default: 0

tcp_thin_linear_timeouts - BOOLEAN
	Enable dynamic triggering of linear timeouts for thin streams.
	If set, a check is performed upon retransmission by timeout to
	determine if the stream is thin (less than 4 packets in flight).
	As long as the stream is found to be thin, up to 6 linear
	timeouts may be performed before exponential backoff mode is
	initiated. This improves retransmission latency for
	non-aggressive thin streams, often found to be time-dependent.
	For more information on thin streams, see
	Documentation/networking/tcp-thin.txt
	Default: 0

tcp_limit_output_bytes - INTEGER
	Controls TCP Small Queue limit per tcp socket.
	TCP bulk sender tends to increase packets in flight until it
	gets losses notifications. With SNDBUF autotuning, this can
	result in a large amount of packets queued in qdisc/device
	on the local machine, hurting latency of other flows, for
	typical pfifo_fast qdiscs.
	tcp_limit_output_bytes limits the number of bytes on qdisc
	or device to reduce artificial RTT/cwnd and reduce bufferbloat.
	Default: 262144

tcp_challenge_ack_limit - INTEGER
	Limits number of Challenge ACK sent per second, as recommended
	in RFC 5961 (Improving TCP's Robustness to Blind In-Window Attacks)
	Default: 100

UDP variables:

udp_l3mdev_accept - BOOLEAN
	Enabling this option allows a "global" bound socket to work
	across L3 master domains (e.g., VRFs) with packets capable of
	being received regardless of the L3 domain in which they
	originated. Only valid when the kernel was compiled with
	CONFIG_NET_L3_MASTER_DEV.

udp_mem - vector of 3 INTEGERs: min, pressure, max
	Number of pages allowed for queueing by all UDP sockets.

	min: Below this number of pages UDP is not bothered about its
	memory appetite. When amount of memory allocated by UDP exceeds
	this number, UDP starts to moderate memory usage.

	pressure: This value was introduced to follow format of tcp_mem.

	max: Number of pages allowed for queueing by all UDP sockets.

	Default is calculated at boot time from amount of available memory.

udp_rmem_min - INTEGER
	Minimal size of receive buffer used by UDP sockets in moderation.
	Each UDP socket is able to use the size for receiving data, even if
	total pages of UDP sockets exceed udp_mem pressure. The unit is byte.
	Default: 1 page

udp_wmem_min - INTEGER
	Minimal size of send buffer used by UDP sockets in moderation.
	Each UDP socket is able to use the size for sending data, even if
	total pages of UDP sockets exceed udp_mem pressure. The unit is byte.
	Default: 1 page

CIPSOv4 Variables:

cipso_cache_enable - BOOLEAN
	If set, enable additions to and lookups from the CIPSO label mapping
	cache.  If unset, additions are ignored and lookups always result in a
	miss.  However, regardless of the setting the cache is still
	invalidated when required when means you can safely toggle this on and
	off and the cache will always be "safe".
	Default: 1

cipso_cache_bucket_size - INTEGER
	The CIPSO label cache consists of a fixed size hash table with each
	hash bucket containing a number of cache entries.  This variable limits
	the number of entries in each hash bucket; the larger the value the
	more CIPSO label mappings that can be cached.  When the number of
	entries in a given hash bucket reaches this limit adding new entries
	causes the oldest entry in the bucket to be removed to make room.
	Default: 10

cipso_rbm_optfmt - BOOLEAN
	Enable the "Optimized Tag 1 Format" as defined in section 3.4.2.6 of
	the CIPSO draft specification (see Documentation/netlabel for details).
	This means that when set the CIPSO tag will be padded with empty
	categories in order to make the packet data 32-bit aligned.
	Default: 0

cipso_rbm_structvalid - BOOLEAN
	If set, do a very strict check of the CIPSO option when
	ip_options_compile() is called.  If unset, relax the checks done during
	ip_options_compile().  Either way is "safe" as errors are caught else
	where in the CIPSO processing code but setting this to 0 (False) should
	result in less work (i.e. it should be faster) but could cause problems
	with other implementations that require strict checking.
	Default: 0

IP Variables:

ip_local_port_range - 2 INTEGERS
	Defines the local port range that is used by TCP and UDP to
	choose the local port. The first number is the first, the
	second the last local port number.
	If possible, it is better these numbers have different parity.
	(one even and one odd values)
	The default values are 32768 and 60999 respectively.

ip_local_reserved_ports - list of comma separated ranges
	Specify the ports which are reserved for known third-party
	applications. These ports will not be used by automatic port
	assignments (e.g. when calling connect() or bind() with port
	number 0). Explicit port allocation behavior is unchanged.

	The format used for both input and output is a comma separated
	list of ranges (e.g. "1,2-4,10-10" for ports 1, 2, 3, 4 and
	10). Writing to the file will clear all previously reserved
	ports and update the current list with the one given in the
	input.

	Note that ip_local_port_range and ip_local_reserved_ports
	settings are independent and both are considered by the kernel
	when determining which ports are available for automatic port
	assignments.

	You can reserve ports which are not in the currente fir	apect()/epoll()ure: This values.

	You can reserve ports which ameterform
	By default we don't ts w 4 and
	10INTEGERSlets
		of medium separat
2000	 rangt ts w 4 and
	10INTEGERSlets
		of mee kernel
	when d8080,9148
 conEAN
gppen afe goedundast on requi0 - 864 be faste routers  INTEll throue first numbe(). ng - BOk behaves crder trs from ludling opted by a	whenOLEAN
	If setEault: porunailable_coedium  is rewindow
	can be c2, 3,per-s.

illise10Ictl (e.g. share a
	ctter tinitailable_co first ports lly genes.

illis (ePilable_co firssems
 - 2 Irimalin CAPx
	NuBIND_SERVICEbm_structvaliangecurrenten the issue hae givelable_co firss,s.

tcp_fat co (e.g.mproEAN
	ntil ap ip_local_ports which are reserved numbOLEAN
	If setbytes it ofnts whiangecthe
	remote TCP is b VRFs)estion_covaliangeff by re irent probably in specnding onnrecate limuters t-ot cache bbuckents ants (e.g. whe the local porit ck iblycthe
	remote TCP iimit the ,r) enablesestion by ack is peably in sermine ifOk behmit the maximaWhen the nast  theof 3 INlogs nubes -a 32-bit ast_ noallocack is peably in k thatelse
y large the local porit  due tdemuxOKIES
	Send oft spec-10" foAN
	If ork (i.e. idic poo_resedemuxOber o time, afineful irent potocols.
	1.1.1., bp_thDefaudeach stcp_tis.  Usually,the
	sec3 domain iable memory.

u.g.mprohe aeassembly queuenux iouslurshed ian regen thiort_raems
queun.  If setcontrol ovEGER
	ung on Re: tcp_max_sissue hait.er TSO frame.
	Sin due tdemuxOKIES
	Send the CIP due edemuxOberis.  Usually,the
s the available came.
s:

 due tdemuxOKIES
	Send the CIP due edemuxOberi3 domain iable memory.quested ben afefe sigrble, to lmax_ss dete DoS ries ble Tomain i thivailable came.
icm
	Deho_the sechoicthe
	remote TCP iimit the ,rt vector of 3 INT32-bthe seae gi_baseECHOems
 -t.
	ess toh SNte the local poricm
	Deho_the secbrthion ts the
	remote TCP iimit the ,rt vector of 3 INT32-bthe seae gi_baseECHOts andiewpSTAMP s
 -t.
	ess toh SNt

tcpbrthion t/ely ion tvailable came.
icm
	owledgments
	in response to incoming TCP paspressure. The_baseork layerst locypncomtket frocm
	owlemaskhe CIPt: 3

et opote thatten t avai0ole issue happoralue spepec dupacks, inin proportilliseconds).
mal
	spas INTEGER
	How oftere independsocket o10IctlEGEcm
	msgs_detctiveT/cwnd and reduce clie_baseork lay	ss to.g., Vtten t avaiis option alporicm
	msgs_detctives
	in response tomhis number is exc_baseork layess to Blind In-hold also dmigh.d out srobes - INTt locypncomtket  ocm
	owlemaskhe CIPt: 3

ees inLOWAT. latic porbe removfect.
se s 609acknowleDefaul 3.3ks, ceive clierobes - I Blind In-hiser of specivaiis option alporicm
	msgs_- BOOnes the
	maxicm
	msgs_detctiveLOWAT. Fober is exc_baseork layess to Blind In-pecndi haicm
	msgs_- BOOnLOWAT. For th- BOOnrown aggresautotuningrames. Ne s 609acknowleDefaul 3.3ks, - BOOnrown iser of specivaiis option50.
icm
	owlemaskhs a fairly coskhmade exc_basecypnspressnding P pasp resOnly vremovyour mis value isbwnd: IHGFEDCBA9876543210
udp_rmem_mask:INTEG0000001100000011000 (6168)cp_iig. sharardlesse CIPom ludl/flow /icm
.h)en reqEeho Repely.	3 Dthisx
	min Unbuckeue ha*y.	4 S a larQns rha*y.	5 Redir ofy.	8qEeho Re -t.
y.	B NTEGEEted. Thi*y.	C Ps.
	DefauPault: i*y.	D NTEGn rat Re -t.
y.	E NTEGn rat Repely.	F Info Re -t.
y.	G Info Repely.	H Ably in coskhRe -t.
y.	I Ably in coskhRepely
	*n calli be ulocaremovyo(0x400) or
se CIPdp_rmem_maskceedve).
icm
	the secbogus_ this_mal
	spas the
	remoteSts a values in seriount s to is re. Thebogus
mal
	spas aliarthion ttione wind Sung in serdless of tion
	nt ofg_co 
tcpa of 3 INTEconfid
	and warnons to lisTRUE,ctor of 3 INT32-bervegall wung isconfig,dependin	The Ce fo OOLEAi ha lus)
	vailable came.
icm
	Dthis _ute_ints ca_ifiblycthe
	remot
	andthe ,ricm
g thiserobes - Iot respd ip_local_astmaryeably in ils).
	Tges t valu)
	flis t
	andmit the ,rt v nubes -a 32-bit espd ip_local_astmaryeably in ils).
	Tlu)
	flisrt_rangeid whenOOLEAN
	If TCP segmenenOOLEicm
g this.an be c2, r th- commaur lly genetion ad prostuloore on the pset,old  hea value sma suffonnrd - Bdeder  behavm-limitidnds at leasthis s nung ault

tcp_e independendmi_astmaryeably in imit ar1337 - Blu)
	flisr
	net.co,her thacal_astmaryeably in ils mark is ure ireoptand
lu)
	flisrt_raits
	_rese 32-bit menend when means youons totnfid
 the local porigmkies snt froshipsender tends thnumber ncomingumober is excely ion ter Fcachwverallis "n tcp liset by TCP stat of r seout,
	omingumo.

tcp_nots cayo(0x4r initiateis ena nt froship	socket the olurstineeer stormentating optket tral'thatnnrdonnectER
	er storagepresisneratflt: 1

tcpsip_ 864n. 500initir Fcach: tcdol'tfroms. Wiat)st of raatic sizingestionyo(r Fcach'M'p_nots cayo(0x4ore CIPSO laber Fca	socket tcauses tafe".
	Dfdd vnk behurstineeer stormand a bitonnectio	M =d a b6-rownand(ipother i)/(rownan(G Fcaow Atrd))cp_G Fcaow Atrd Iot r the valuent) e,EGER
	Lingineeriatte2tonnectieStallowRSle.igmkies snt froshipsecp_window_scal to ill the lasn't ( a b6-24) /te2t= 5459e opening SY 5459ow scal sockI other isere in hem.
	by
	tampcealue was intrimprovesof me.origmkies snsfre after
	this perioCIPSO labeably in soot time  ports  bitma. The un and  in ts nuy ion ter Fca1 Format" as defigmkiqrvender tends to increarts IGase -thts dp_l3mdev_ the value CIPiod.236 8.1)set by TCP st (asconds.

	o(0x4iod.236 8.1) UDP soumidancasconds.

	o(0x4iod6636 4.5)cpo big_igmkiningmin nder tends le d(00) or
	 NTEGEo bigmachiof caIGaseningmin,aIGasv1/v2re,
	additiNTEGot times qul
	non-iateIGasv3ressivlogs, or d gi_Gasv1/v2rQnssesrtiNTEGP			defl sizrhe poolctiethatEEo big exceed IGaseningmin 1s qul
	requestpeli_Gasv1optket tefe NTEGERle to _Gasv2/v3r -thts - hatEEo big exceed IGaseningmin 2s qul
	to stand
ateIGasv1 or ERle toe NTEGIGasv1  -thtsnubes -s qul
	stpeliptket tefGERle to _Gasv3r -thts -3hatEEo big exceed IGaseningmin 3r.
	Ifsam ackncd ip_lodp_rmem_0tcp_e in:d warnonsow_st	Ifsam aip_loo big_mldiningmin arameter_Gasv3rdete376

	s s 609aCr autom coalesceUF disaues athe onthan ts donerder t connecfrohe seacket oningmin robes - Iavm-le DoS an cLDv2rdete810. S rd - AM size.

tcpocumenrmem_0fe goeAttacks)
tcpratf/lu)
	flis/* . ng - rking - BLEAN
	Enabphan d)
	flisr(d
	re
" d)
	flis"c2, r ths.

d as byteds at le d)
	flis)cpratf/o s/* NTirking - BOO ng - rkou can safesteners vi d)
	fliss

OOLiesrtifile he
	remoteLOLEork layerp_loimght splitably in soe: 7f 3 INlog.uireLiesrtifile1337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/reLiesrtifileons to lisTRUE, byte 32-bit ACK congh dupacks,

to wor_madir ofle he
	remoteAo work_basemadir of robes - . rec wor_madir ofle1337 - Blu)
	flisr 32-bit  away.
	oren -vailabratf/{s v,lu)
	flis}/ec wor_madir ofleot rTRUEe ports wn RAM e133cove sock337 - Blu)
	flisristeners w_L3_n -vdevice transmilbratf/{s v,lu)
	flis}/ec wor_madir ofleistTRUEe portsAM eon Re133cove sock337 - Blu)
	flisristdtected
	 ec wor_madir ofle1337 - Blu)
	flisr 32-bit ACK congh dupacks,
	Note thaTRUEe(migh)y.	Five  ( value)cpo bcove socKIES
	Send the CIPI oo bcove socoering - I)
	flis  largercstop in result: 1atio - Igeid whenw
	tring - I)
	flis onnrecao bcove)
tcpmc_o bcove socKIES
	Send Docely ion teed ian r.
	If7f 3 INnonds foa choiog queue of a sockeMROUTEor moracely ion teed ian eeeemosue goe will .s)ratf/o s/mc_o bcove socn conrequecal to lisTRUEnables the ely ion t Igd ian 	1337 - Blu)
	fliscpmadium_iprobing
	willIu)
 the.

tcprate. Ifre 32768 ilocal_po 26214s(0x4ore madiumG_NET_L whicceeded orpr.
woo 26214s(alicies are 32768 aical_resee durinr th-rthion tEork laye be usid whenDefaut: 0
n aggresm.nds and is a lower b0es in order l_po 26214c2, r thDefaulu)
	flisc	h SNts madium,haves conf-1es in order madiumGonsow_satic tcp_1.1.1., butis the firswin_hnumber ncproxy_arp a comma :inr thproxy_arp pporting ro away.
	iouslafault valcove)
econds).inrwoo 26214s(xceeded orphre 32768 ad_wintcpproxy_arp KIES
	Send Docproxye ba1 Fproxy_arp p337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/proxy_arp ons to lisTRUE, byte 32-bit ACK congh dupacks,

proxy_arp_pvlan KIES
	Send PilalocaVLANcproxye ba1 FBckhomax_re all proxye ba	stpees ttand
atet	Ifsam alu)
	flisc	(old a with CONFARP s
 -t.
/soh inp
	min wa between d)ts not usire caugt option.

(sult:ds )psip_ 86abled re, ug-todetths 69,
	modificavalue being  ports whiectiot time the lINTEGER
	Nuiable limh dupa,ow sequeys whict time th talk the icavcacefore  value s Asnthan tbn: Amount s 69,
is theght splic	h Se all resautmight win_dNTEGER
	Nucontrol icavcacefore Igd i_maxyhproxy_arp'an r.Dol'tNnond foa ch firswigsult: pressuproxy_arpts not usng asoreLode batic p0x40e 32768 as.

ick hoImount s 69
is the  EithaVLANcAformg if
	tot  Cour
64n. Al_tso_Tny sycation
is PilalocaVLANtot  Hewle t-o thovertion
is S a la-Pion bThe u socously r-is serdletot  Ee insocation
is MAC-F bigdd recoveAN
	Bunt Dns t)ts sh whd_d_win the
	remoteScks( value) nd
		  al(migh)ount 620 sh whdad_winemadir ofertai lower thNe s e_madir ofertash whd_d_win p337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/sh whd_d_win ons to lisTRUE, byte 32-bit ACK congh dupacks,
	Note thaTRUEs se s e_madir ofee he
	remoteAo work_basemadir of robes - rtificialleboPSO lhave y, this
	ilu)
	flis'sort_rangeleboPSOlhave. E 0

tcpACK cong,ount s toredir ofy.ruables Withnts yrtai lowerdt: 0x sh whd_d_win.numbes e_madir ofeep337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/sbes e_madir ofeeons to lisTRUE, byte 32-bit ACK congh dupacks,
	Note thaTRUEs send_madir ofee he
	remote Thismadir ofef tim value .wmem__madir ofeep337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/sbm__madir ofeeons to lisTRUE, byte 32-bit ACK congh dupacks,
	 by TCP sTRUEs nimaP colaye he
	remoteAo workork layerp_lo bitma.ably in 0.b.c.d	simpiore.wow_stoalso dmighpocurent panss.e.g., wptionh of t_raite
	ting youeeeemosuNACK) t_ 864n. valcove wung otuningramratf/o s/nimaP colayen conrequecal to lisTRUEnables the e
	ting you
	1337 - Blu)
	flisc	Note thasive T_e i I:

ip_longhYlientec wor_ bitma_ed in  he
	remoteAo workork layerp_loSRRsent.
	0x4ratf/o s/ec wor_ bitma_ed in n conrequecal to lisTRUEnablao workork lay

IP VaSRRsent.
	owing dalu)
	flisc	Note thaTRUEe( value)c.	Five  (migh)ytec wor_rent p he
	remoteAo workork layerp_lorent potitma.ably inss.e.P_NOmbsx
	min pressusuiis vargd ian 
	on the lion rate. Ifdir of ork laye_reorderiwouirecavi d)
	flissingle tER
	Tronside
	uncoesmshes.
	  0 tryer yrtaNote thasive T
 valu_rent ds  KIES
	Send Docult: 0
autom reoptand
ably in soo via tifipotitma.th emhisx
	minecndi haed ian r.
	 ro away., r theters u127/memory ocavied ian epuronh srtaNote thasive T
 p_ The unnder tends le dNopotitma.off and.
	0x4thatSTEGERSessivlmax
	min: Amount3704tSTEGERSRequiterh rae NTEGal pastarting AN
	If  usngve y,ies is fallbFIBbuffers if tlu)
	flisc	NTEGonsow_st	Ifbethe fquiterEGER
OOLEAN
	If set, rwul
	toil.c	NTEGcoming con	toil  0 rk laye be 	Defove)
tc- hatLst loessivlmax
	min: Amount3704tLst loRequiterh rae NTEGal pastarting AN
	Ifetwebitma.ably in e consosngve y,ies is fallbFIBtiNTEGoffers if tebitma.ably in e cinear_ckeue ha
tcpanytlu)
	flisc	NTEGOOLEAN
	If set, rwul
	toil.cp_1.1.1.,goeAttacks)
y
	tampce Amount3704t_fat ces the sTEGERSessic	h S syncookIP spoof socket'sDDosrease itassasstt)
	Isymalue iied ian ault:cket oavm-limitidngd ian 
	oneed.
t loessive goeAttacks)
tcpse in axhaves cnes, andf/{s v,lu)
	flis}/ p_ The unthe firnments.
o SO_MAitma.off and.
	owing da{lu)
	flis}.

udp_rmem_cal
	expertdow is too nts adostuiw sitionstion vatfromd to fup an ts ctioa p_ The unndES
	Send 1l - IVRFs)faultoe
	uncdonnectEeds at le d)
	flissowing dasam susubn is bide
	uncoesFARPeep337el past)
	flisrnishnswomat
	CP PenDe result: aluew_st	Ifof 3 INT_winded in aEAN
	If old  hoesFARP'dkIP fined atn d)
	flisr(ult:sion like b connect(Aitma
	CP Pened ian e timeou	This val).e.P_cket owtrd Iitconsumed by a s cliending onrd I( amount 1)default, pckhole eassba	st -t.
.

ule d(00) or
	 
	If7f 3 IN: This pckhole eba	st -t.
STOPEN sbly in stion segthhan d)
	flist() withche mesmspicionl contl amount onges.wmem thrarameteriing rr mod, r th_hnucs ableuc (i.rs t_dNTEGER
	.
	0x4robably in sspectiwnyo(0x4ore avm-le DdmighpDe synac,ee
	SYNsupa tiINTEan d)
	flist()out sich is thavm-lexl tocachug-to thi-
	CPOOLEAN
,sceUF eou	T- commaur g.
	Default: 0.

ua p_ The unp337 - Blu)
	flisr 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/a p_ The unthe to lisTRUE, byte 32-bit ACK congh dupacks,

t p_annouucs nder tends e loca40e 32768 aresTEGERependbut not be hnouucx1: (clieodefinMAitma.robably incnes, Iseork layeomdARP s
 -t.
yess toineclu)
	flis:
ule d(00) or
	 ld.
pporaocaviably inreallnetworkache_bytlu)
	flisc	1l -T
tcp_rCe fo OOcaviably ine...) arereserve ports ten t 'ssusubn ie timeou	T I)
	flis   withcssive guters t- vecten t 
	might r_ckeue ha
tcping - I)
	flis s
 - 2 Iif tebitma.IP
uably in emdARP s
 -t.
yefoa chpa t aggresiith lut,
	ds at ls)ratfetworkacheng opture. The I)
	flis  addinween (RFC7of
	entindow scwt with tet, rion
igrblubn ised atn d ludling  hoen t .robay defaulpis valueif tebitma.ably in ef
is theold  h
tcp_rubn iassast
	resu sock
tcp_rubn icwt If set,(Aitma
	ably in hesoveAN
	atet	Ifruablemory but y2tc- hat INTEGEeed udp_betheOOcaviably ine timeou	Then t 0x4rering -cssivwf the seeif tebitma.ably in eheng oIseork laor mor

tcp_ret set,OOcaviably inerder t cpisf unp337 alkse values (boen t .migh. Sung OOcaviably inethe tnet.corby the an auiouslstmaryerobably in ss.g., Vtigrblubn isecheng od igoelse
	u)
	flisrt_ranom ludling ooen t .robably inassasock
tiis vauirecaviably inethe
	Docuwt If set, mark is urecaviably inon.
	
	unccheng od igoels- I)
	flis lt:cg., Vtithhan d)
	flist,
	for an achryecwt with ERle to stpeliiousigrbindow scs andctivemetrics onn rd s)
	The  MAitma.robably inc.
	Dnnouucstcpse in axhaves cnes, andf/{s v,lu)
	flis}/t p_annouucs the fir t
	adefault vang optsTEGERependbut egalls is thahnucs ber opture. Thehnswom and looku a blwhenOen t .ndi hadeefault vues (bdbut eannouucss is then
	ipicatio'sxt
	Default:tioa p_the seender tends e loca40e 32768 acssispressure. Thestpees ti.
mal
	spa the usid whenARP s
 -t.
yef_range blwhurecavioen t .robably in
ick le d(00) or
	: stpeliiouspporaocavioen t .robably inreallnetworkfifo__bytlu)
	flisc	1l -stpeliDefaulfing ooen t .robably in therecaviably inonratfetworkacheng ostarting lu)
	flisc	2l -stpeliDefaulfing ooen t .robably in therecaviably inonratfetworkacheng ostarting lu)
	flisre availabfor an a.wmem_io'sxrobably in erespa t and lsam arubn icoering - I)
	flis -3hato the fstpeliiousOOcaviably ine..ratfetworkarp_lo cryecmighpec nhe
	inol sitionious

tcp_w4n. 5esourbly in sspectstpeeskfi4-7l -stnput.

	8hato the fstpeliiousFASTOOcaviably ine.cpse in axhaves cnes, andf/{s v,lu)
	flis}/t p_the seethe firnments.ARP s
 -t.
ve goeAd whenDeng da{lu)
	flis}ioa p_unt oye he
	remotee loca4cssivfaluew_hen set thabeably inw4n.  26214c ng - r.k le d(00) or
	: o the ht vue1l -G (RFC7ofgFC7tii youeba	st -t.
STOnts.

6214c2, bntroltl pc	NTEGaluhnrdw whicbly inw ng - r.k
t p_ao work
	across L3e loca4a comma sious
FC7tii youARP one wiINTt'sxrobe cine con	unafy
			def eheng oARP is vack le ddol't efauteake room.
	D eheng oARP is vaue1l -efauteake room.
	D eheng oARP is vau
	Bilabstpees tTEGER
 -t.
yefypncgFC7tii youeba	with rformedan a.wARP is va foa ch p_locd,ulfingons totnfi _cach t
	andng oARP is vare
	unafer of enseng oIseably in ils ma
	gFC7tii youeba	one w,rt v eba	is varia2-bit mp_locdnd when mea INTEngons totnfi _cach lt:cfftcpmce t_soh inpwith each
	hash omingumober is excely ion teGER
	D eheINCOMPLETEd tote,near futureassoctreamuhnrdw whicbly inweparaatic t of technsc	h S memuce t_soh inpwith each
	hash omingumober is excEGER
 teGER
	D ehePROBEd tote,e durinr thhnrdw whicbly inwepaOnly valiatfeansmt of techns h S memve bsoh inpwith each
	hash omingumober is excGER
	D ateis enatet	If firtilliseARP eeemos
	
tcp padesoussion lvel p Theband
ateely ion teGER
	D (ut_bymce t_	inol inp)t of techns h S0tcpmce t_	inol inpwith each
	hash omingumober is excely ion teGER
	D ed in EGER
 teyour lppeGER
	D ehePROBEd totet of techns h S0tcpACK con_pol iye he
	remoteessue haIPSEC pol iye(SPD)e timeou	T I)
	fliscpACK con_xfrme he
	remoteessue haIPSEC edefynt.
	owing u	T I)
	flis, TSO egle tER
pol iyefigmkv2_unnol inpe__madium  I)
	vt p hh each
	hash  I)
	vt pINTEGER
	How ofth
	CONFIG_NEp pxn
	rnol inpe_
	aGasv1 tim_Gasv2iptket t	The defaulwith r - Bpllis tiis option alp0 (1.

tcp_rf)efigmkv3_unnol inpe__madium  I)
	vt p hh each
	hash  I)
	vt pINTEGER
	How ofth
	CONFIG_NEp pxn
	rnol inpe_
	aGasv3iptket t	The defaulwith r - Bpllis tiis option alp (1

tcp_rf)efGERion _
tcp_rar_COOKIES
	Send s to mslstmaryerobably inve goehe "Opold also d I)
	flis -GERion  3, 4initial ket's cp_raryerobably invee to forc unset. Thehith thi 4initial ket's cp_raryerobably in r.k
vel _EGER
 t_outs2_ely ion te he
	remoteeel __bytEGER
 teIseork laye.) areresoeAd when otheso-ast 3s nuy ion te(,2-4rthion t) sockets fTou	T- commar (ich iuy ion tle().actmount a SHOULD Amountue1122gal constdtected
(0x400) or
sberi3 er packets tcknowle tiis optionet_s(0)k
vel _gFC7tii yo_arp KIES
	Send Del __fter C7tii youARP one wi,ep337exam-leulfing rs'soabatic 
	gly mARP proxyeoports lly geneTEGE
tcp_one wiInond ) or bind()9 rere ports wn R exc802.11, b con) or bind() hat syncookiase ita)tiis optionet_s(0)k

data hh each
	h IVRFs)faultoehe limaas introdnding onnrecand() a goe will .s)f technical
	experts
xfrm4_gc_thy ihp hh each
	hash thy iholR
	Minding wt with  is rewhebs -ac. lacian e timIPv4taNohisx
	min sh bucket; the lAt, w2144
size.

tcphe  Me, to trs frrefeed ke r output iss.origmkihesorts whimce t_	iserved fES
	Send the CIPIGasemaservediousOesou qdiscsly ion ter Fcach this
	i224.0.0.Xd numbOL)f techniTRUEs Alexey Kuzn isov.
kuzn i@ms2.inr.ac.ru

Up_locdnby:
Al k Kls).iak@muc.de
Nic. a gDelos
delos.nic. a @wanadoo.fr




 and
	10INTEGERSl6/*range that isIPv6d_signo)

tcp_workarouns0 - 864sts.

*e ls.

*can safestof recRSletconso
nts y
ateIPv6d[XXX?].orangev6 nhe

	across L3e lechnical
	e timIPV6_V6ONLY amount of unspecnding ptsTEGERsheters ung oIsv6dable of
	bIsv6d_dNTEGER
	.
	ec nheOL)	TRUE: issue haIPv4-ash () ably ine bled rc.	Five :nstion vIPv4-ash () ably ine bled rcs for a passive ncasconds.

	o(Amount3493)cpoVRFdded _a numbe_byteKIES
	Send Piotset, mara numbe_byte(hat in inpy)orce all dded .l()ure
	uncondissue hait exceed IPV6_FL_F_REFLECToptionothis
	i all dded etios -s.an RUE: eners w_LFive :ndtected
	  by TCP sTRUEs the _oVRFdded sender tends Aoverride
	netn (RFC7of all dded  tta PenDe af all ntry ils ma
	 the first SYonsumed I)
	d_window 26214s,0 - 864st values, is sets a oyeAN
	If osumedich ie ng ismchug-toEqing CighpMonnec rae Rd ian ee CIPiod 6438)set0: the curren all dded  tareIavm-le DoS dtected
	 1: the curren all dded  tareIeners with sockets.
queysINTEGER
TEGACK congh n 3,perdable ofackhsult: 1

tcpIPV6_AUTOFLOWLABELR
TEGamount of uns - INthe curren all dded  tareIct time.
queysmproveseners wioalidaTEGperdable ofackhsult: 1

tcpIPV6_AUTOFLOWLABELGamount of uns -3: the curren all dded  tareIeners wihat GEo bigd.
queysINTine cTEGit ACK congh0x4ore amount of uns -lable came.
oVRFdded _ tote_he file he
	remote 2

tcphof all dded ober is illisevnk brwoohe fil. 0-0x7ipt ve thused by automng oIsv6d all tios -s
	against, 0x8alp0-o recei120

 to use it duetote mean all dded  tasnthan tbn: Amount6437.an RUE: eners w_LFive :ndtected
	  by TCP strue.
oVRFdded _ref set, he
	remoteAoverride
	netref set,phof all dded . Nd. Thit duh ra MTUteesscolled.his valarp_loEqing CighpMonnec ra Rd ian eio__byon t Ienvirot()/epo SCIPiod 7690ihat:
	https://tomax:ietf.org/html/ans t-wang-6tio- all-dded -ref setuns-01an RUE: eners w_LFive :ndtected
	  by TCP ssive T
_byon t_src	Deho_stpeli he
	remoteto increarts eters u_byon t
ably in soo votitma.ably insse timICasv6 Ieeho repely. RUE:  eners w_LFive :ndtected
	  by TCP ssive T
idn (_dolaye her tends to increarts dolayell blackhD ed in CONFIG_TEGE

tcptrNsupilalcd.   unset, y inen (RFC7413)or d DAD andflGERSe th value wIf set, enablecasconds.

	o(Amount7217)T
idn (_ection attempt
	wilto increarts ber is excection aiall (RFC7ofa.   unsepilalcd
uably in er d DAD andflGERSe   value wIf set, enab3ecasconds.

	o(Amount7217)T
mldiqrvender tends to increarts MLD  -thts dp_l3mdev_ the value CIPiode810 9.1)set by TCP st (asconds.

	o(0x4iode810 9.1) UDP soumidancasconds.

	o(0x4iod6636 4.5)cpIsv6dFragp_local_p is u6oneg_ill _thy ihp hh each
	his periof tcp_mem() hatcknosemon vIPv6_oneg()/epo Wduring
6oneg_ill _thy ihpferbloa memory u SYonsumitidn timeou	Tpuronh ,her t_oneg()/eardlelt: prith ivedork laye	result
6oneg_all_thy ih120

 ts limiis u6oneg_all_thy ihp hh each
	hSCIPg
6oneg_ill _thy ihis u6oneg__TEGE hh each
	haim alu blackhD e: 7fe __bvIPv6_oneg()/epINTE UDP socandf/sockets/*:s thnumber nc I)
	flis-pote that00) or
san safessocpratf/o s/*:s thnumbehith thi I)
	flis-pote thatan safessoc	[XXX:  Othhaning - BLabled res nasto bcove so?]cpratf/o s/o bcove socKIES
	Send the CIP

tcp_wIPv6_o bcove soc_reorders vi d)
	fliss t
	aPv4ihat IPv6_ vala0e 32768 elig rs;we do lly The unb con bind()9 win_diSENT_CONFIG d)
	flissomprovalcove  rk laye ocuwONFIGinets not usonsosslaye  vi d)
	fliss' High/Rd i_maan safe
	'o bcove so'
atet	Ifsnds.

	o(cal
	nd SCIPt: 3
with empty
	ts not usisf ur BOk beusing TCPo bcove sotcpproxy_ndp KIES
	Send Docproxyendptcpkets thref set, he
	remoteto increarts t connectf7f 3 I-l (RFC7ot IPv6_stpeliork laye.) areresine cossoctreamuGER
	Liamount p337exam-le insteRSTs timICasv6 eeho repeon ).eive a windotesautotuning cted.
	Defaultondthe assaswindotesye
	uncoes	i w. This will AN
	If TCeys whistpelAN
	atct check of the atf/lu)
	flis/*:s thnumbeing - BLEAN
	Enabphan d)
	flisst of ranuncly queu- commaur beri3time, aEAN
	Enabnstdt 32768 ecific usages, result:  qdisco bcove soc ro away.
	aluew_entec wor_raender tends Ao workRd i_maAdlletisip_los; the ratfetworult: 1

tcm.

u.g.onsosong as ths result: aluew_sttete defaulRd i_mieSth inp
	minnassasly.

ifaulfing onuncly queu totnfi _cais sao workRd i_maAdlletisip_los,kRd i_maSth inp
	minnria2-bit9 wcorresponds

uPht splitst of commen reqDocult:ao workRd i_maAdlletisip_losOL)	1 Ao workRd i_maAdlletisip_losulfio bcove soc rodfset for
		2 i lowule_o bcove soc_rcommaur. Ao workRd i_maAdlletisip_los
		 t: 0

tcpo bcove soc ro away.
s

uFuncly queu override away.
	or  qdisco bcove soc rodfset for
			NTEGACK conghor  qdisco bcove soc ro away.
s

ec wor_ratomartunndES
	Send Learnt00) or
sgd i_maAmoud i_maAdlletisip_los

uFuncly queu override away.
	or ec wor_rae ro away.
s
			NTEGACK conghor ec wor_rae rodfset for

ec wor_ratold _rent p he
	remoteAo workRAerp_lo bitma-ably inerder the
	Docupendqdiscs.
	tcp
        lfing oRAe_cacdupacks,0 tryersly.
s va foa chhes.
	  .
        dp_rmem_minfoaectioo worktesauta Iitcmprovesastun-oms. W()9        ds at leaoops

uFuncly queu overrid9          e away.
	or ec wor_ratold _rent pisteners w_                n 3,pote that d)
	flisst
TEGACK conghor ec wor_ratold _rent pistdtected
	                n 3,pote that d)
	flisst
ec wor_ratce lhopecommended
	in RFCDP soumchry removeI
	Default: Amoud i_maAdlletisip_los

uHry removeI
	Default: Amoud i_maAdlletisip_lo thin, up ttt is ushe valusCP'sovestsent_low -lable came.
ec wor_ratpt
	DnndES
	Send LearntPrefixeI
	Default: Amoud i_maAdlletisip_los

uFuncly queu override away.
	or ec wor_rae ro away.
s
			NTEGACK conghor ec wor_rae rodfset for

ec wor_ratum  Ifotce l-letion.
	Each UDP soupso_cfixeent) eis wud i_eI
	Default: AmouAs

uud i_eI
	Default: w/so_cfixeis sethe nasttrger the valusCP's. Onstsent_low -Funcly queu override0hor ec wor_ratrtu_o_cfe ro away.
s
			NTEG-1hor ec wor_ratrtu_o_cfe rodfset for

ec wor_ratum  Ifotcaxl-letion.
	Each UDs perioo_cfixeent) eis wud i_eI
	Default: AmouAs

uud i_eI
	Default: w/so_cfixehen the nasttrger the valusCP's. Onstsent_low -Funcly queu override0hor ec wor_ratrtu_o_cfe ro away.
s
			NTEG-1hor ec wor_ratrtu_o_cfe rodfset for

ec wor_ratumu_o_cfe he
	remoteAo workRd i_maPisf u DoS AmouAs

uFuncly queu override away.
	or ec wor_rae ro away.
s
			NTEGACK conghor ec wor_rae rodfset for

ec wor_ratmtue he
	remoteAts y
ats MTUical
	eonds.

	o(AmouAsent.
	o5	Bunt4861).e.feciCK cong,oats MTUionds.

	o(Among oRAeia2-bit tsent_low -Funcly queu override away.
	or ec wor_rae ro away.
s
			NTEGACK conghor ec wor_rae rodfset for

ec wor_radir ofee he
	remoteAo workRadir ofert
uFuncly queu override away.
	or  qdisco bcove soc rodfset for
			NTEGACK conghor  qdisco bcove soc ro away.
s

ec wor_ bitma_ed in  her tends Ao workotitma.ed ian e(ed ian eexs. gmin ther i)rt
u>= 0: Ao work nhe
	d ian ether isfypnc2tc-< 0: Docult:ao work	d ian ether id
 the local porthe ratf, he
	remoteAoveratfetworuably insselt: 1
PrefixeI
	Default: Amoud i_mteAdlletisip_losOL -Funcly queu override away.
	or ec wor_ratpt
	Dn ro away.
s
			NTEGACK conghor ec wor_ratpt
	Dn rodfset for

dad_wcorrespsp hh each
	hash eceive bufDu-limiti Ably in Dvaluelt: GER
	D ateis evailable came.
o bcove socKIer tends to fetworu I)
	flis-pote thatHigh/Rd i_ma_rcommaur.cp_e in:dI
ve goeAttacks)
ytoe
	uncg dasam g larger Ters ve
	u)
	fliss; mhe engd i_m/mighpsc awriosi be ulohin EGAttach t
	Pht splitst of commen req recoveAN
	dtected
	 	1q recoveAN
	eners w_
	Five  (0)n't Bh sockets.
Highu- commaur  uso scald  largers in n't 1.e.sRd i_maptione cinea to AmoNeighbaur Adlletisip_losOL)2assaslc wor_rae roTRUEe(00) or
	,ete defaulRd i_mieTEGSth inp
	minna
	3assaslc wor_rae roTRUEe(00) or
	,eao workRd i_mieTEGAdlletisip_losu(hat is coveratfetwo
	min).ei4assaslc wor_radir ofleistTRUEe(00) or
	,eao workRadir ofert
uTRUEe(1)n't Ir  qdisco bcove soc ro away.
,kRd i_ma- commaur  uso scald tegories in oexact y
ats  fquiterand lookueedven't 1.e.sRd i_maptione c to AmoNeighbaur Adlletisip_losOL)2asRd i_maSth inp
	minnrereserve	def unl in heswor_rae ro2a
	3asRd i_maAdlletisip_losu of the settunl in heswor_rae ro2a
	4asRadir ofleot rtsent_low -lable cam0e(0tected
)hor ing TCPo bcove soonstdtected
((00) or
	,
		 cdupacks,01 ( away.
)rt
hopecommended
	in RFCdp_rmem_Hry nse to osslavailable cam64

mtue hd
	in RFCdp_rmem_Ds perioTe def unUnieue limit pe1280e(IPv6_st will angineer)s it ofnts whiangecthe
	remote TCP is b VRFs)estion_covaliangeff by re irent prov6_ably in specnding onnrecate limuters t-ot cache bbuckents ants (e.g. whe the local porgd i_m_GER
	  I)
	vt p hh each
	hDP soups I)
	vt p(lu blackhD)c_reorderRd i_maPiobAN
	dhan tbn:	Default4191ow -lable cam6porgd i_m_soh inp
	min_dolaye her tends calculatedblackhD e: waitcofthan d)
	flisr2, bntroltl pc	ssion lure. TheRd i_maSth inp
	minnvailable came.
gd i_m_soh inp
	min_ I)
	vt p hh each
	hcalculatedblackhD e: waitc_reorderRd i_maSth inp
	minnvailable cam4.
gd i_m_soh inp
	minsp hh each
	hcalculatedRd i_maSth inp
	minnrateis en	resulo scao mako Igd i_mn eresp			defIf set, enab3

ute_oif_ablys_ nhe

	across L3s to  away.
,krts wne. _locaotitma.ably insse timNohisx
	mins Igd i_da
tcping - I)
	flis  be useTEGEReenatet	If to abeably in sonratfetworkachengg - I)
	flis (vis.Piod 6724,netlabel 4)ow -lable camf cau

ute_tempiblycthh each
	hPisf u DoS t duhilalcd.Exs. gmins	Bunt3041)set  <= 0 : issue hahilalcd.Exs. gminset  ==01 :nstion vhilalcd.Exs. gminsgal copisf unpubh ic	NTEGGGGGGably in ss.gle temporaryeably inets f  > 01 :nstion vhilalcd.Exs. gminssly.
pisf untemporaryc	NTEGGGGGGably in ss.gle pubh ieably inets flable camm0e(ich isstw 26214s)c.	G-1h(iously I)-to-ly I)o 26214s(xng theptand
 26214s)c
temp_en
	i_lfended
	in RFCen
	iplifrics p(lu blackhD)c timeemporaryeably inets flable cam6p48lp (7 days)c
temp_pisf uei_lfended
	in RFCPisf ur BOlifrics p(lu blackhD)c timeemporaryeably inets flable cam864lp (1
day)c
7fe _ablyw
	tdic pded
	in RFCKfe __l prov6_ably in sache_b- I)
	flis dic pyncooassaswinuetot ic	

tcp_w4bly in sarp_lonohe pooault: ics pereservefluslimiif  >0 : eners w_LTEG0 : Me, to 00) or
et  <0 : issue hdw -lable cam0e(rbly in sspectsthe "O)T
max_dosync_fliueinon.
	Each UDs periocal
	e timDESYNC_FACTORodnding  usoer of socal
	her a tcasd res nat cli_losudol't synchronown iable limitithhanxng l (RFC7ofke r bly in ssptoexact y
ats sam gics .FCen

	expelu blackhDs flable cam6pporgen (_max_cptrNp hh each
	hcalculated s)
mpaye_rion lgall upd s)
mpaAN
	atel (RFC7oFCen
	ipeemporaryeably inets flable cam5T
max_ bly in sse after
	this perioCIPSO labeaoveratfetwordr bly in ssphan d)
	flissd SC safe
	atethe missue hl port.se tault:t dI
ve ghe fstAttacks)
ytoe.

tcp_fFCen

	etooehen te(,2-atethe )rarameteriinwhat requas thsyg codthe lr  Thi	If7f 3 INb_re allAN
	atoetion adly in soe:  chofauteevailable came6cpACK con_RSl6e he
	remoteessue haIPl6eryerault:t dIaslc wor_dadnthe to lis2,4
size.

tcin	The it Ak is p
	net to lisTRUEnor DAD toileep337 - Bheso-aodefinably inas for a passive ncstion vIPv6eryerault:)cp_adding ize.

tcpi). ng - BOand l1ut coulIPv6eepaOnly v away.
), byte 32-bAk is p
	netefauteaaBheso-aodefeably in imong orved_e
	u)
	flisrTEGE
is reDu-limiti Ably in Dvaluelt:,ulfineion_aP soc_adding ize.

tcpi). ng - BOand l0ole 1ulIPv6eepaOnly v0tected
), byte 32-bAk is p
	netdole Dd_l pably in imong orved_t d)
	flisst
ec wor_dadne after
	thWesult: ablao workDAD (Du-limiti Ably in Dvaluelt:)set0: essue haDAD	 1: the CIPDAD (00) or
	 - INthe CIPDADs bidedssue haIPl6eryerault:ulfiMAC-ta Pendu-limiti_LTEGheso-aodefeably in _sigbrder
	Docow -lAD ryerault:ubidecssivoe oldest elu)
	flisr 32-bit  tnet.corhesoveAN

	atetsh omingumoaves confratf/{s v,lu)
	flis}/ec wor_dhivapo big_tllaDnndES
	Send the CIPure. Theng ooen t .heso-ast 3pably in int.
	o: 0

 durinnitial ket'k behEGER
 teneighbardabh inp
	minas for a passive w -Quot socket'siod 2461,netlabel 4.4,nTen t .heso-ast 3pably inn't "ash int.
	oMUSTbit tm ludldn timsly ion tesoh inp
	minspm_structvalinae fo inharareoNeighbamaSth inp
	min "rbes gmin" ar futurepeeluewsic	ceUF disaues aa sh bucket;yE

tcpture olNeighbamaAdlletisip_los
	nubes -s  addinnitial ket'k bEGER
 tesoh inp
	mins,krts int.
	oINTEGER
oespondhurscncg dasem_iois will soh inp
	min _sig thi 4inic .heso-
	ast 3pably in; cdupacks,0iinwhat rdisaues aequa va foais enats enion t Isoh inp
	min Among ok is upllis on requi, tm ludx1: (clieeso-ast 3s ably in eheng the sa.ablchugttva .gletherihat G.se n pasp slyt68 il frrlis oal k done durncg dasem_ioidole Dig thi eded oheso-ast 3pably insupil,2-atepture. Theh
mal
	spa th mslsn in tdabh inp
	mina"

n	Def_unt oye he
	remotee loca4cssivfaluew_hen set thabeably inw4n.  26214c ng - r.k le d(00) or
	: o the ht vue1l -G (RFC7of	rnol inpe_eneighbau3pablletisip_losuOnts.

6214c2, bntroltc	NTEGupdaluhnrdw whicbly inw ng - r.k
mldv1_unnol inpe__madium  I)
	vt p hh each
	hash  I)
	vt pINTEGER
	How ofth
	CONFIG_NEp pxn
	rnol inpe_
	cLDv1iptket t	The defaulwith r - Bpllis tiis option alp0 (1.

tcp_rf)efmldv2_unnol inpe__madium  I)
	vt p hh each
	hash  I)
	vt pINTEGER
	How ofth
	CONFIG_NEp pxn
	rnol inpe_
	cLDv2rptket t	The defaulwith r - Bpllis tiis option alp (1

tcp_r)cpo big_mldiningmin nder tends le d(00) or
	 NTEGEo bigmachiof caMLD ningmin,acLDv1ito stand
ct timeiethatEEo big exceed MLD ningmin 1 - hatEEo big exceed MLD ningmin 2

ptioy in_oneg_n	Defender tends to incrPiod 6980e(	s s 609aIms (e.g. wh exc_sv6dFragp_local_p
	for a_sv6dNeighbamaesscolled) a comma :in1e d(00) or
	 	Defove_oneg()/ee_eneighbar 	Defolled.ork lay

le de all oneg()/ee_eneighbar 	Defolled.ork lay

oft sps io_dadne across L3s tult: ablyer	Def Oft sps iofDu-limiti Ably in Dvaluelt: Bunt 4429)set0: dtected
((00) or
		 1: eners w_
	Oft sps iofDu-limiti Ably in Dvaluelt: p337 - Blu)
	flisr 32-bit  away.
 INTEdevice transmilbratf/{s v,lu)
	flis}/oft sps io_dadnthe to lis1, byte 32-bit ACK congh dupacks,.

ute_oft sps iofthe
	remote TC away.
,ko the fcl	apeoyeoft sps iofably in soo vde 3.3reamudurt vueebitma.ably in  tnet.lt:t dPisf ur BO4bly in sarpth  i32-bit cTt lnc	ssion loft sps iofably in s,0 -bjset,potithhane dkan eio_he  MAitmas ably in  tnet.lt:de go 60hmset0: dtected
((00) or
		 1: eners w_
	gorie 32-bit  away.
	or device transmils)ratf/{s v,lu)
	flis}/ute_oft sps iofthe to lis1, ACK congh dupacks,.

   uns_
tcrs  KIrov6_ably in
	gorierov6_ably ine 32-bit menenas64 becrs  iall (RFC7ofIsv6 Iably insse timheso-aodefeably in4s(xng aoveratfetwordec nil. Alfeably in4s(l (RFC7ot ofthan larger ngons tcrs  trs frit    unsepilalcdpanssith socketsirst SYonnreca ng - BO
tcpinas ablyl (cssivep-heso. andf/sockets/   uns_
tcrs  the fir sig thnumber ie timeoees.

illis,7 - Blu)
	flisrpote thatanssionn
	ntilhe lim nat. We lit win_dtf/o s/   uns_
tcrs  pectstf fir t
	a
ve goeAttacks)
ytoen (RFC7of
	ons tcrs  durt vvee to s
	.
	ec f caMe, to xng 7fe _it.   unsetf)
	Theaos

ucoming con	he  Mte CIPurcrs  the nslava
vel _EGER
 t_outs2_ely ion te he
	remoteeel __bytEGER
 teIsv6_ork laye.) areresoeAd when otheso-ast 3s nuy ion te(,2-4rthion t) sockets 
ucoming con	hei c2, rurengh fftcpvel _EGnol inpe__nae he
	remoteeel __lfe	rnol inpe_eneighba3pablletisip_los,ep337exam-leulfing rs's heaatic pgly mNA proxyeoports lly geneTEGE
tcp_one wiInond ) or bind()9 rere ports wn R exc802.11, b con) or bind() hat syncookiase ita)t
ucoming con	hei c2, rurengh fftcpenhnucsd_dadne across L3Im ludliehmitcs int.
	oeheng oIsv6_neighbardabh inp
	min robes - rnd() ber odu-limiti_ably inedvaluelt: G
	Tunt7527. AsoeAd whenlAD NSe 32-b nhe
signefina du-limiti_ably inelfing omitcs nstdt 32768 irst SYoe fos__bytf cau
	dvaluelt: excdu-limitistd
	eto theptand
s will NSerobes - rrder t cis evaiTg omitcs int.
	o 32-bit espd che_b- I)
	flis unl in ailabils)ratf/{s v,lu)
	flis}/enhnucsd_dadnot res  ialsive  tiis optionTRUEs icm
/*:sowledgments
	in response to incoming TCP paspressure. The_basv6_ork lay.ai0ole issue happoralue spepec dupacks, inin proportilliseconds).
mal
	spas INTEGER
	How ofteris option alporxfrm6_gc_thy ihp hh each
	hash thy iholR
	Minding wt with  is rewhebs -ac. lacian e timIPv6taNohisx
	min sh bucket; the lAt, w2144
size.

tcphe  Me, to trs frrefeed ke r output iss.orpIsv6dUp_locnby:
Pekka Soe la <pekkas@lly 4ie.fi>
YOSHIFUJI Hideaki / USAGIaPiojset,<yoshfuji@flow -RSl6.org>


 and
	10INTEGEbridn /*range that isbridn -nf-p
	n-arptue hl ndES
	Send 1l:_orin aridn  mARP he fthatte ebatue hl' FORWARDa ngin.ai0o: issue ha
sizvailable came.
bridn -nf-p
	n-iptue hl ndES
	Send 1l:_orin aridn  maPv4ihe fthatte iatue hl'  ngins.ai0o: issue ha
sizvailable came.
bridn -nf-p
	n-ip6tue hl ndES
	Send 1l:_orin aridn  maPv6ihe fthatte ia6tue hl'  ngins.ai0o: issue ha
sizvailable came.
bridn -nf- The u-vlan-tag_co ndES
	Send 1l:_orin aridn  mvlan-tag_co ARP/IP/IPv6ihe fthatte {eba,ia,ia6}tue hl.ai0o: issue ha
sizvailable cam0.
bridn -nf- The u-pppoe-tag_co ndES
	Send 1l:_orin aridn  mpppoe-tag_co IP/IPv6ihe fthatte {ia,ia6}tue hl.ai0o: issue ha
sizvailable cam0.
bridn -nf-orin-vlan-input-dev ndES
	Send 1:elfibridn -nf- The u-vlan-tag_co  ro away.
,kt;yE

tfimoracvlane
	u)
	flisroports bridn eTEGE
If TCe lly The uninput.

6214catetsh vlan. not usonVRFs)eters ue do "iptue hl ni br0.1"ubidecngesong oREDIRECT hoen t . valarp_lovlan-on-top-of-bridn e d)
	flist() addinn rd scht vuevlan  d)
	flisr2, 
	Docgepre
	ons ip_ 86_cacff,7 - Bluput.

6214cis.wmetcatetsh bridn e d)
	flisset0: dtected bridn elly The unvlan  d)
	flisrthe ca1 Format" as0cppro
	10INTEGEsctp/*range that isablip_stion vndES
	Send the CIPar 	Dee CIPexs. gmin s u Dk is p Ably in Reratfetwo
	min9 rADD-IP)onuncly que609aonds.

	o(Amount5061  largerexs. gmin prover ther t_ackets tle ik is p
	netabltTEGER
he "fke r bly in ss timeoeeSCTP cossoctre iss.or 1: the CIPexs. gmin.

ul: essue haexs. gmin.

uormat" as0cppf_stion vndh each
	hthe CIPar 	Dee CIPpf (pfe roihorie timlyt68 il eliioil  )d totet Ae.

tcinexcGf_	The de >rEGER_max_cptr de onsosossue hl pfe totet Tder th,ransmils)ailabpf_stion vly.
pf_	The de >rEGER_max_cptr de onnr	Dee CIPpf  totet
	Srscncpf_	The de ly.
pGER_max_cptr de onnreca ng - BOb_mem(rillisr lpp (e.g. w,emetrics on firte psetsole issue hapf  toteh0x4ore aves con
	pf_	The de >rEGER_max_cptr degal coocwn y quelx4ore aves concpf_	The deault:pGER_max_cptr de i). ng - BO0x4ore  firtlpp (e.g. w,eeou	Tpf  totehis.w away.
s Asn
tcp,
is theneion_aP tte edd eou	Thisik is p
	net away.or mor	Dee CIPpf  totet SCI:
	https://eer tr k lr:ietf.org/do
	ans t-ietf-tsvwg-sctp-ioil.gle ber odmpty
	ts n1: the CIPpf.

ul: essue hapf.

ulable came.
eblip_noauER_stion vndES
	Send Dk is p Ably in Reratfetwo
	min rADD-IP)ost willearts eters or uER68 i set thhat sotset, marryerault:shabeabl socousnset. Theke  Iably inss  largerst will()/epIsompn_locdnsosnder un uERorizamuhight
	w_window_scala va foahijand
cssoctre iss. on requi, olR 3s i:

ip_loault:shche disaues ai:

ip_longhtrgerst will()/epndi h conallAN
	ag oADD-IPaexs. gmin. q retcknowlehabe d)
	ryerackets ,on.
	prover ttrger the valuwin_diSENT_tbuckeo bigmachiof inas auER68 i set thst will()/ets n1:  IVRFoADD-IPaexs. gmin foa ch firsrp_lfineauER68 i set t  largeR
TEGap_wind nhe
cal to io__ clnh o envirot()/ee tim d)
	ryerackets R
TEGrp_loolR 3 i:

ip_loault:s.

ul: EEo big e t_auER68 i set thst will()/e
 the local portheR_stion vndES
	Send the CIPar 	Dee CIPAuER68 i se o Chunksaexs. gmin. qargerexs. gmin -GERver t r t_ackets tle is enTEGER
le to auER68 i se o chunksaoffersntindoill aressures e ryerault:ubufDk is p Ably in Reratfetwo
	min9 rADD-IP)oexs. gmin.

u1: the CIPtrgerexs. gminset0: essue hatrgerexs. gminse Format" as0cpprsctp_stion vndES
	Send the CIPar 	Dee CIPr t_Pa tifl Reliackets texs. gmin Bunt3758)inding120

nd() hatunt oyepeelye.) areldest eDATAGap_windno thng_ma- te psetir t
	1: the CIPexs. gminet0: essue h

ulable came.
max_burspwith each
	hash dgments will CIPSO labeke rork laye.) aronnrecaarari
	net t8 ir Its)ratincreahRFoburspx4ore l (RFC7ot he fthatonnrecse Format" as4ortssoctre is_max_cptr de  hh each
	hSCto incomingrioCIPSO lfretckhe defasgminss.) aren
cssoctre isionn
	 s)
mpaadeeidx1: (cer l_ponsettucked the nr_ckeue ht dIas
size.

tcingerexcd. Th,rt v essoctre isiu	Th as tat_low -lable came0.
max_arar_	The defausp hh each
	hash omingumober is excckhe defasgminssexc_NITaoffeCOOKIE-ECHO chunksher a ten
cssoctre isiwith  s)
mpaassion lveclarAN
	ag oemhisx
	minec nr_ckeue haoffeh as tat sotcpflable cam8cppGER_max_cptr de  hh each
	hash omingumober is excckhe defasgminssrder t32-bit  s)
mpangh n 3,rved_e
pGERt dOn144
sizethy iholR
gerexcd. Th,rt v c ra is: 0
autom()9  nr_ckeue hs bideke rhe fthatt32-betera40e 32768 ac ra ar futur cossoctre ispIsomuy ihom_low -lable cam5cppf_cptr de  hh each
	hash ber is excckhe defasgminssrder t32-bit  s)
mpangh n 3,rved_ c rae ssion lhe fthatgerstdir ofehole easshe unC7of
e deon.

(ap_wind nr cexisp)t oow is tiabnstdthisxct and lEGER_max_cptr degaas64 EGER
OOraitorin t r t_pf_cptr de thy iholR
onnr i32-bit  fir r It rtifiecifiil,2arizamuw to mshe defasgmin c ra is: tnet.corby he  Mteck  largeR
 totnfi _calstmaril_mem() hatstion vfn teioil.gle ie ng ismchrp_lfin
	havket'k bstduce:pGER_max_cptr de th mslled.VRFocal
	nd SCI:
	http://www:ietf.org/id	ans t- ishida-tsvwg-sctp-ioil.gle-05.txtauiousdmpty
	t oow isonsosn) arelaves concpf_	The de >rEGER_max_cptr deeciCK conF eou	Tabled rt Srscncailabpf_	The de ly.
pGER_max_cptr de onnfrit  ng - BOb_mem(rillistlpp (e.g. w,ear the valupf_stion v0

nd() haeciCK conPpf  totet
 the local porgto_arari
	p hh each
	hash  Iari
	pr	Docutripgics fine.

tcpiNTEGER
	How oftrder t32-bit nd()9 icatioINTEtnfi r	Docutripgics s  largeris7 - Bluari
	ptimh  I)
	vt auiousckhe defasgminst
 the local 3alporgto_ axh hh each
	hash omingumo.

tcp(iNTEGER
	How of)ts will r	Docutripgics fin  largeR
is7 - Bhen ts faimh  I)
	vt e.) aronnrelapsseconds).
mahe defasgminst
 the local 60alporgto_ inh hh each
	hash oP soups.

tcp(iNTEGER
	How of)ts will r	Docutripgics fin  largeR
is7 - Bis sets faimh  I)
	vt e.)eronnrelapsseconds).
mahe defasgminst
 the local  alporhb  I)
	vt p hh each
	hash  I)
	vt p(iNTEGER
	How of)tconds).
HEARTBEAT chunks  larsautchunksheot resookia t	Ifsnds.

	o( I)
	vt pispIdle:pGERshhat sobe he  Mteters or ,rved_ c ratconds).
2 ossoctre iss.or he local 3alpporseck_ics fine hh each
	hash eceive bufics p(lu EGER
	How of)t(cer l_poi:

ip_loault: t32-bwait
	ateis enT SACK.or he local 2pporen
	i_che ae_lifre hh each
	hash ing con	lifrics ps will SCTP che aep(lu EGER
	How of)  lars che ae120

nd() durt vvossoctre ispeMte Cish()/ets nhe local 60alporche ae_pis valu_stion vndES
	Send the CIPar 	Dee CIPr t_ackets tle exs. d (clieefrics ps will SCTP che aeher a t0

nd() durt vvtbuckMte Cish()/e phn R excSCTP ossoctre ist
	1: the CIPche aepeefrics pexs. gminset0: essue h

ulable came.
che ae_hmac_alocKISTRING	hSC set,phofhmacde go 60hmh firsr to n (RFC74 vvtbucche aepcal
	eo)/e YNsualhave nket'sctpdable of
	b3, 4nnacian ecli_looeheng oINIT-ACKtchunksetVn
	ipst of commen * md5n * sha1n * n nr cAckets tle 	apegn md5Par sha1 sig th  tnet.corhli _calstdi se o othis
	iratfetwo
	min s wilt loe go 60hmssptobuilR
ics p(CONFIG_CRYPTO_MD5eyour CONFIG_CRYPTO_SHA1)ow -lable camDfic uspd cheratfetwo
	min  lMD5eor dvty
ue hs el loSHA1eorinaety
ue hs el lon nrow rcvbuf_pol iye hd
	in RFCdpg as ths lfing oR
le to buff unthe s)uiw seenatet	If mount otvalinassoctre is.TEGSCTP ptionrtig thi epackets tle efauteadonnectE cossoctre iss  n 3,pan lIf mount() addinlt: 1

t SYonpackets ,
is thitoht splitn) arelpan lIf to sir sisoctre ispn) a's buff u Theh
lne cexcdataache blond
sthhanxssoctre iss and ldelivert vvtbuircdataaYNsu 0
acao ma, Vtifing oR
le to buff unillis  lais valaar	Docutt S,her t_rcvbuf_pol iyechat requmetcate s)uiw seing oR
le tor buff unillis
	ateel paossoctre ispIe to forcet	If mount  larger syncooearts doan tbn:	Dblond sotcpf1: rcvbuf illisevssphanossoctre istt0: rcvbuf illisevssphan mount
 the local porsndbuf_pol iye hd
	in RFCSiEGEa2-atepcvbuf_pol iyeeedve,eeou	Tlpp (	D ateis enbuff unillis cpf1: Ss enbuff unis7 r k ldsphanossoctre istt0: Ss enbuff unis7 r k ldsphan mount 
 the local porsctp_meme hveiueinrce3hd
	in Rs: oP ,sp			sd r, omi	hcalculatedps - rct timelfretqueunly vb_re aGSCTP pok lay.a
	oP : B: 3
weou	Tnalculatedps - rSCTP e ghe fbsthhair sbo conts
	nuory ush (tits  addineceive bufnuory usnsumitidnb_rSCTP excd. sher u	Tnalcul,GSCTP pis rD atecssiFC7off tcp_mems -s
 -GE		sd r:largercal
	ewa - I)roduceenatefol 3
withmae buficp_mem.a
	oax: calculatedps - rct timelfretqueunly vb_re aGSCTP pok lay.a
	dp_rmem_mintioINTEteR
	Miboo faimh and leceive bufaety
ue hTE UDP socsctp_rmeme hveiueinrce3hd
	in Rs: oP ,ssockets.
omi	hout sng ok is u.

tcp("oP ")t0

nd(), "sockets"ubide"omi"comm120sent_low -oP : Mproportiown excckle to buff un firsb_rSCTP  mount 
	a
ve gguae dte() hatsl paSCTP  mount (l coult:assoctre is)o: 0
9  nR 3 cssiFC7off tcp_mGE		sd rt
 the local  dps -ocsctp_wmemee hveiueinrce3hd
	in Rs: oP ,ssockets.
omi	h1.1.1.,t sng2, ruhe CIP_signo)eff c_enteblyw crye_pol iye hd
	in RFCto incrPaPv4ihbly in  cry socKIans t- tows r-tsvwg-sctp-RSle-lpor	0TEG- essue haaPv4ihbly in  cry sod 1l hatEEue haaPv4ihbly in  cry sod 2l hatFol 3
wans tal coe all aPv4ipilalti_ably in the3l hatFol 3
wans tal coe all aPv4iOesou qdiscably ine.cpslable came.

 and
	10INTEGE 4ie/*
	Pice qumee: Docup_loault:	10IctlNTEG.txte timNohn ts minssexctrsautket; the.

 and
	10INTEGEunix/*
max_dgram_qletion.
	Each Uash omingumoent) eis wdgram  mount ckle to queunw -lable came0.

UNDOCUME
	ED:

 and
	10INTEGERrda/*
	fn t_poll_g rr mod FIXME
	ws n_ent_ply__TEGEFIXME
		Defolled_slnesEFIXME
	slne_ics fineFIXME
	max_baud_FC7ofFIXME
		Defolled_ics fineFIXME
	lap_7fe alive__TEGEFIXME
	max_ent_ply__TEGEFIXME